# SDD scratch (task briefs/reports/ledger) - internal, never shipped.
.superpowers/

# Built bundle output (Task 18).
dist/

# Python
__pycache__/
*.pyc

# Per-run artifacts created when someone runs the CLI. Legitimately contain
# a customer's own CRM data; never belong in this public repo.
/metrics.json
/crm-report-card.html
/run-config.json

# Test runner + OS cruft.
.pytest_cache/
.DS_Store

# Internal design docs (specs, plans, release runbook) are kept outside this
# public repo. Nothing under docs/ ships here.
docs/

# raw provider payloads are large; kept locally, not committed
benchmark/raw_payloads/
