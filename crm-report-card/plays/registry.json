{
  "version": 1,
  "plays": [
    {
      "id": "employee-count-accuracy",
      "unlocks": "employee_count_accuracy",
      "object_type": "company",
      "label": "Employee-count accuracy, verified vs stored",
      "requires_roles": ["domain", "company_size"],
      "providers": ["icypeas_find_company_url", "exa_answer", "apify_run_actor_sync", "ai_inference"],
      "cost_per_record_usd": 0.02,
      "default_sample": 100,
      "comparison_rule": "mismatch = stored count and the LinkedIn associated-member count fall two or more size bands apart. LinkedIn associated members counts profiles that list the company, which runs higher than payroll headcount."
    }
  ]
}
